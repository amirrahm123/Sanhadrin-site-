# https://www.robotstxt.org/robotstxt.html
# Allow all crawlers — no blocking rules.
User-agent: *
Allow: /

# The Sitemap line is rewritten at build time with the real origin taken from
# the built canonicals (scripts/gen-sitemap.mjs), which also appends
# `Disallow: /admin`. This static copy is the fallback if that step is skipped —
# keep it in sync with SITE.url in src/data/site.ts.
Sitemap: https://sandrine-events.com/sitemap.xml
